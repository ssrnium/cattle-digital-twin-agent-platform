# 奶牛平台全栈验收报告（2026-09-14）

> 范围：首场验证版切片（20号牛棚/103头牛/爬跨+跛行/告警工单/断网恢复/牧场智能体）的真实运行验收。
> 结论：**核心链路已通过，可进入真实模型权重接入阶段**（遗留项见第 6 节）。

---

## 1. 架构与调用链

```
cow-edge 模拟器 ──MQTT──► Mosquitto(1883) ──► cow-admin(8081)
                                                 │  事件入站：schema 校验 → event_id 唯一约束幂等 → 落库
                                                 │  → TwinUpdater（迟到不回退）→ TaskRuleEngine（24h 去重建单）
                                                 ├─→ PostgreSQL(cow_db, 13 表) / Redis
                                                 ├─→ Outbox → MQTT 工单下发通知
                                                 └─→ /api/v1/agent/* 代理+审计 ──► cow-agent(8003)
cow-web(5174) ──/api──► cow-admin                cow-agent：BearCode 运行时 + 6 只读工具 + create_work_order(需审批)
cow-ai(8001)：mock 推理（权重插入点）
```

## 2. 实际执行的命令与真实结果

| 命令/动作 | 结果 |
|---|---|
| `git init` + 基线提交 | 232 文件（`9208e24`） |
| 便携件：PG 16.10 / Redis 5.0.14 / **Mosquitto 2.0.22**（官网安装包静默安装到用户目录） | 全部可用；Mosquitto 订阅/收发实测通过 |
| `mvn package`（cow-admin，含测试） | **Tests run: 8, Failures: 0** + BUILD SUCCESS |
| 启动六服务 + broker | 全部 /health 200 |
| `simulator.py --mode online --duration 22` | 11 事件 → DB：events/twin/timeline/工单全落地 |
| `simulator.py --mode offline/reconnect` | 9 事件缓存（HIGH 5/LOW 4）→ 优先级补传 → 重发去重"duplicate alerts = 0" |
| `cow-agent pytest tests/` | 16 passed |
| `npm run build`（cow-web） | ✓ built in 11.75s |
| `tools/acceptance/cow_d1_main.py` | **15/15 通过** |
| `tools/acceptance/cow_d2_agent_confirm.py` | **7/7 通过** |

## 3. 浏览器级功能验收表（D1，Playwright 实测，截图在 tools/logs/shots/cow-*.png）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 未登录重定向 | ✅ | /login?redirect=/barn |
| 2 | 登录+看板 | ✅ | 牛总数 103 渲染 |
| 3/3b | 牛棚二维孪生图 | ✅ | 103 圆点 + 15 头异常变色（发情橙/跛行红） |
| 4 | 单牛详情+时间线 | ✅ | 孪生状态卡 + timeline 2 条 |
| 5 | 事件流 | ✅ | MOUNTING/LAMENESS 实时列表 |
| 6a-d | 工单状态机全流程 | ✅ | NEW→DISPATCHED→PROCESSING→PENDING_REVIEW→CLOSED，version 0→4，DB 逐步核验 |
| 7 | 设备页 | ✅ | edge-node-01 在线 + 心跳/pending |
| 8a | 智能体查询（多轮） | ✅ | 工具真实调用、回答带依据 |
| 8b/8c | 写操作人工确认 | ✅ | 弹窗展示工具与参数 → 批准 → 工单落库（26→27，NEW，关联正确牛只） |

## 4. 机制与异常验收（API/DB 级）

| # | 场景 | 结果 | 证据 |
|---|---|---|---|
| M1 | 重复 event_id（HTTP 重发） | ✅ | 第二次 duplicated=true，DB 仅 1 行 |
| M2 | 错误 deviceKey | ✅ | 401"设备凭证无效" |
| M3 | 迟到事件（event_time 早于状态时间） | ✅ | 只追加 timeline，twin_state 不回退 |
| M4 | 断网缓存与恢复 | ✅ | offline 缓存 9 条 → reconnect HIGH 优先补传 → pending 归零 → 重发去重 9/9，重复告警 0 |
| M5 | 写操作确认（cow_d2 七步） | ✅ | pending 拦截→参数正确→批准受理→落库+1→NEW 关联正确→审计落库 |

## 5. 已修复问题及修改文件清单（验收中抓到的真 bug）

| # | 问题 | 修改文件 | 修法 |
|---|---|---|---|
| 1 | **jsonb 写入 500**（device.meta，同黄瓜的 stringtype 坑） | `cow-admin/.../application.yml`、`docker-compose.yml` | JDBC URL 加 `stringtype=unspecified` |
| 2 | **事件契约 DTO 不接受 snake_case**（MQTT/HTTP 上报全挂）+ 缺 ingest_time 字段 | `modules/event/dto/EventIngestRequest.java` | 每字段 `@JsonProperty("snake_case")` + 补 ingestTime（对前端的 camelCase API 不动） |
| 3 | **admin 智能体代理 DTO 丢 session_id**（多轮对话每轮新建会话，确认链路断裂） | `modules/agent/dto/AgentChatRequest.java`、`AgentConfirmRequest.java` | `@JsonProperty("session_id")` |
| 4 | **BearCode 母版工具执行缩进 bug**（执行块嵌在 per-tc 循环里，N 个调用重复执行 N(N+1)/2 次，DeepSeek 报 duplicate tool_call_id） | `cow-agent/bear/agents/agent.py`（vendor 副本） | 批次构建+执行块移出循环，统一执行一次；母版原目录未动，改动已在代码注释与本文档声明 |
| 5 | **DeepSeek 思考模型 reasoning_content 未回传**（含工具调用的后续请求 400） | `cow-agent/bear/agents/agent.py`（vendor 副本） | 流式增量收集 reasoning_content 并随 assistant 消息入历史 |
| 6 | cow-agent 领域工具 HTTP 客户端被系统代理劫持（login 非 JSON） | `cow-agent/app/cow_tools.py` | `trust_env=False` |
| 7 | cow-agent 单测编译挂（MP 3.5.7 BaseMapper 重载歧义） | `src/test/.../EventIngestServiceTest.java`、`WorkOrderServiceTest.java` | `any()` → `any(Class)` 显式类型 |
| 8 | 智能体写操作 prompt 死锁（过度取证/拒绝调用） | `cow-agent/app/cow_agent.py` | 写操作闭环规则改为"取数≤2 次→说明→确认→立即调用" |
| 9 | 双保险补丁：tool_call_id 历史消毒（防 DeepSeek 复用 id） | `cow-agent/app/patch.py` | 运行时 patch（响应出口 + 历史成对重写） |

## 6. 尚未解决的问题（均不阻塞）

1. AI 推理为 mock（等第一阶段真实权重：爬跨 YOLOv8n、跛行 YOLOv11+RTMPose+XGBoost）；
2. 智能体建单对话成功率依赖话术（模型谨慎，需按演示脚本两步走）；机制本身已确定性验证；
3. Docker compose 未验证（本机无 Docker）；
4. amqtt 纯 Python broker 在本场景订阅被拒（QoS 128），已改用 Mosquitto 便携版——compose 里本来就是 mosquitto 镜像，无影响；
5. 断网演示的心跳超时翻转（60s OFFLINE）未在浏览器长等验证（机制代码已读，设备页 pending/心跳字段已在断网恢复流程中间接验证）。

## 7. PROJECT_STATUS.md

已在项目根建立（版本 v0.2.0-acceptance；三段式；已知问题表；验证命令速查）。

## 8. Git 变更摘要

- `9208e24` 基线：奶牛平台骨架+智能体层（验收前快照，232 文件）
- 本次验收提交：见 `git log`（9 项修复 + PROJECT_STATUS + 本报告）

## 最终结论

**当前核心链路已通过，可以进入真实模型权重接入阶段。**

依据：七服务真实互联；MQTT 事件→幂等→孪生→工单→智能体→人工确认的完整闭环在浏览器级与 API 级双重验证通过；断网缓存恢复演示达成"优先级补传 + 重复告警 0"；9 个验收中发现的真 bug 全部修复并复验。
